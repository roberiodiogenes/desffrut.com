# Desffrut PDV Desktop — dependências
#
# Instalar no Windows (dentro de um venv, de preferência):
#   python -m venv venv
#   venv\Scripts\activate
#   pip install -r requirements.txt

pywebview==5.3.2        # janela nativa — no Windows usa o WebView2 (Edge Chromium) já embutido no SO,
                        # não precisa do extra [cef] (cefpython3 não tem wheel para Python moderno)
pywin32>=306            # win32print — impressão raw (cupom ESC/POS, etiqueta TSPL/ZPL, inkjet)
pyserial==3.5           # leitura da balança via porta serial (RS-232/USB), substitui Web Serial API
requests==2.32.3        # chamadas HTTP para a API do site (pareamento de dispositivo, health check)
pyinstaller==6.10.0     # geração do .exe (só necessário na máquina que compila)
